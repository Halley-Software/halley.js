/**
 * This file contain all the types that the Reply object need
 */

'use strict';

/**
 * A type wrapper for primitive and non-primitive data types
 */
export type body = string | number | boolean | object | Buffer;